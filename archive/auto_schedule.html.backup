{% extends "base.html" %}

{% block content %}
<div class="mb-6">
    <h2 class="text-2xl font-bold text-gray-800 dark:text-gray-100">Auto Schedule Configuration</h2>
</div>

<div class="grid grid-cols-1 lg:grid-cols-2 gap-6" x-data="scheduleManager()">
    <div>
        <div class="bg-white dark:bg-gray-800 rounded-lg shadow-md p-6">
            <h5 class="text-xl font-semibold text-gray-800 dark:text-gray-100 mb-4">Schedule Times</h5>
            <form action="{{ url_for('update_schedule_times') }}" method="POST" id="scheduleForm">
                <div id="timesList">
                    {% for time in schedule_times %}
                    <div class="flex items-center gap-2 mb-3">
                        <input type="time" class="flex-1 px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent dark:bg-gray-700 dark:text-gray-100" name="times[]" value="{{ time }}" required>
                        <button type="button" @click="removeTime($event.target)" class="bg-red-600 hover:bg-red-700 text-white px-4 py-2 rounded-lg transition duration-200">
                            <i class="fas fa-times"></i>
                        </button>
                    </div>
                    {% endfor %}
                </div>
                
                <button type="button" @click="addNewTime()" class="w-full bg-gray-600 hover:bg-gray-700 text-white font-medium py-2 px-4 rounded-lg mb-3 transition duration-200 flex items-center justify-center">
                    <i class="fas fa-plus mr-2"></i> Add Time
                </button>
                
                <button type="submit" class="w-full bg-blue-600 hover:bg-blue-700 text-white font-medium py-2 px-4 rounded-lg transition duration-200 flex items-center justify-center">
                    <i class="fas fa-save mr-2"></i> Save Schedule Times
                </button>
            </form>
        </div>
    </div>

    <div>
        <div class="bg-white dark:bg-gray-800 rounded-lg shadow-md p-6 mb-6">
            <h5 class="text-xl font-semibold text-gray-800 dark:text-gray-100 mb-4">Scheduler Status</h5>
            <div class="space-y-3">
                <p class="text-gray-700 dark:text-gray-300">
                    <strong class="font-semibold">Status:</strong> 
                    {% if scheduler_status.active %}
                    <span class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-green-100 text-green-800 dark:bg-green-900 dark:text-green-200">Active</span>
                    {% else %}
                    <span class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-yellow-100 text-yellow-800 dark:bg-yellow-900 dark:text-yellow-200">Inactive</span>
                    {% endif %}
                </p>
                <p class="text-gray-700 dark:text-gray-300">
                    <strong class="font-semibold">Last Run:</strong> 
                    {% if scheduler_status.last_run %}
                    {{ scheduler_status.last_run }}
                    {% else %}
                    Never
                    {% endif %}
                </p>
                <p class="text-gray-700 dark:text-gray-300">
                    <strong class="font-semibold">Next Check:</strong> 
                    {{ scheduler_status.next_check or 'Not scheduled' }}
                </p>
                <p class="text-gray-700 dark:text-gray-300">
                    <strong class="font-semibold">Last Status:</strong> 
                    {% if 'Error' in scheduler_status.last_status %}
                    <span class="text-red-600 dark:text-red-400">{{ scheduler_status.last_status }}</span>
                    {% else %}
                    <span class="text-green-600 dark:text-green-400">{{ scheduler_status.last_status }}</span>
                    {% endif %}
                </p>
            </div>
        </div>

        <div class="bg-white dark:bg-gray-800 rounded-lg shadow-md p-6">
            <h5 class="text-xl font-semibold text-gray-800 dark:text-gray-100 mb-4">Manual Control</h5>
            <form action="{{ url_for('run_scheduler') }}" method="POST">
                <p class="text-gray-600 dark:text-gray-400 mb-4">
                    Click the button below to run the scheduler manually.
                    This will process all pending schedules immediately.
                </p>
                <button type="submit" class="w-full bg-yellow-600 hover:bg-yellow-700 text-white font-medium py-2 px-4 rounded-lg transition duration-200 flex items-center justify-center">
                    <i class="fas fa-play mr-2"></i> Run Scheduler Now
                </button>
            </form>
        </div>
    </div>
</div>
{% endblock %}

{% block scripts %}
<script>
function scheduleManager() {
    return {
        addNewTime() {
            const timesList = document.getElementById('timesList');
            const newTimeGroup = document.createElement('div');
            newTimeGroup.className = 'flex items-center gap-2 mb-3';
            newTimeGroup.innerHTML = `
                <input type="time" class="flex-1 px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent dark:bg-gray-700 dark:text-gray-100" name="times[]" required>
                <button type="button" class="bg-red-600 hover:bg-red-700 text-white px-4 py-2 rounded-lg transition duration-200">
                    <i class="fas fa-times"></i>
                </button>
            `;
            const btn = newTimeGroup.querySelector('button');
            btn.addEventListener('click', () => this.removeTime(btn));
            timesList.appendChild(newTimeGroup);
        },
        removeTime(button) {
            button.closest('.flex').remove();
        }
    };
}
</script>
{% endblock %}
